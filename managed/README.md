# Managed Artifacts

This directory holds generated Midnight build outputs for `CredentialRegistry.compact`.

The repository includes a deterministic manifest generated from the contract source so the New Moon submission has a stable, tracked managed folder before the Midnight toolchain is run.

When the Midnight compiler is available, replace or augment these files with the official generated artifacts for preview or preprod deployment.
