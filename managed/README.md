# Managed Artifacts

This directory currently contains only a source-audit manifest for
`contracts/CredentialRegistry.compact`.

It does not contain compiled Midnight circuits, proving keys, verification keys,
or deployment output. Generate those with the official Midnight Compact
toolchain before deploying to preview, preprod, or testnet.
